using UnityEngine;

[ExecuteAlways]
public class GridOccupant : MonoBehaviour
{
    //[Tooltip("Đặt đáy chạm lưới khi snap.")]
    //public float bottomExtraOffset = 0f;

    //[Tooltip("Tự snap và tránh đè ô khi bạn di chuyển trong Editor (không Play).")]
    //public bool autoSnapInEditor = false;

    //void OnValidate()
    //{
    //    if (!Application.isPlaying && autoSnapInEditor)
    //        GridMap.TrySnapUniqueInEditor(this);
    //}

    //void Reset()
    //{
    //    // tiện: mặc định bật auto-snap khi add component
    //    autoSnapInEditor = true;
    //}
}
